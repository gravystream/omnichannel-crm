# =============================================================================
# Omnichannel CRM - Environment Configuration for Hostinger
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# =============================================================================

# =============================================================================
# REQUIRED - You MUST change these!
# =============================================================================

# Database password - use a strong password (32+ chars recommended)
DB_PASSWORD=CHANGE_ME_use_a_strong_random_password_here

# JWT Secret - use a 256-bit random string
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_32

# MinIO credentials - use strong passwords
MINIO_ACCESS_KEY=CHANGE_ME_minio_access_key
MINIO_SECRET_KEY=CHANGE_ME_minio_secret_key

# Grafana admin password
GRAFANA_USER=admin
GRAFANA_PASSWORD=CHANGE_ME_grafana_password

# =============================================================================
# AI CONFIGURATION
# =============================================================================

# AI Provider: openai, anthropic, or local
AI_PROVIDER=openai

# OpenAI API Key (if using OpenAI)
# Get from: https://platform.openai.com/api-keys
AI_API_KEY=sk-your-openai-api-key-here

# Model to use
AI_MODEL=gpt-4

# =============================================================================
# CHANNEL INTEGRATIONS (Optional)
# =============================================================================

# Slack Integration
# Create app at: https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_SIGNING_SECRET=your-slack-signing-secret
SLACK_CHANNEL_PREFIX=incident

# SendGrid (Email)
# Get from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your-sendgrid-api-key

# WhatsApp Business API
# Configure at: https://developers.facebook.com
WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id
WHATSAPP_ACCESS_TOKEN=your-access-token
WHATSAPP_WEBHOOK_VERIFY_TOKEN=your-verify-token

# Twilio (SMS/Voice)
# Get from: https://www.twilio.com/console
TWILIO_ACCOUNT_SID=your-account-sid
TWILIO_AUTH_TOKEN=your-auth-token

# =============================================================================
# ADVANCED CONFIGURATION
# =============================================================================

# Server
NODE_ENV=production
PORT=3000

# CORS - comma separated list of allowed origins
CORS_ORIGIN=https://crm.yourdomain.com

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Session
SESSION_SECRET=another-random-secret-string

# Logging
LOG_LEVEL=info

# =============================================================================
# EXTERNAL SERVICES (if not using Docker containers)
# =============================================================================

# PostgreSQL (if external)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Redis (if external)
# REDIS_URL=redis://user:password@host:6379

# Kafka (if external)
# KAFKA_BROKERS=broker1:9092,broker2:9092

# Elasticsearch (if external)
# ELASTICSEARCH_URL=https://user:password@host:9200
